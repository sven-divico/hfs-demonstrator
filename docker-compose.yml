services:
  hfs-demo:
    build: .
    container_name: hfs-demo
    ports:
      # Listen only on the host loopback so the host-native Caddy
      # (which owns :80/:443 + TLS + basicauth) is the only path in.
      - "127.0.0.1:8080:8080"
    restart: unless-stopped
